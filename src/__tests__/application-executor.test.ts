import {describe,expect,it} from "vitest";import {classifyApplicationRoute,planApplication} from "../commerce/application-executor.js";import {PlatformRegistry} from "../commerce/platform-registry.js";import {Web3CareerAdapter} from "../commerce/web3-career-adapter.js";
describe("application executor",()=>{it("classifies common ATS routes",()=>{expect(classifyApplicationRoute("https://boards.greenhouse.io/acme/jobs/1")).toBe("greenhouse");expect(classifyApplicationRoute("https://jobs.lever.co/acme/1")).toBe("lever");expect(classifyApplicationRoute("mailto:jobs@example.com")).toBe("email");});it("requires owner identity for employment ATS applications",()=>{const p=planApplication({applyUrl:"https://boards.greenhouse.io/acme/jobs/1",title:"Full-time staff engineer",automatedSubmission:"approval_required"});expect(p.employment).toBe(true);expect(p.requiresOwnerIdentity).toBe(true);expect(p.preserveApplyUrl).toBe(true);expect(p.submission).toBe("approval_required");});it("rejects insecure application links",()=>expect(()=>planApplication({applyUrl:"http://example.com/job",automatedSubmission:"allowed"})).toThrow("REQUIRES_HTTPS"));it("stages the original Web3 apply URL without modifying it",async()=>{const a=new Web3CareerAdapter(new PlatformRegistry().get("web3-career"),{});const url="https://jobs.lever.co/acme/123?source=web3career";const r=await a.submitApprovedAction({type:"submit_proposal",idempotencyKey:"job-123",payload:{applyUrl:url,title:"Contract API integration"}});expect(r.status).toBe("approval_required");expect(r.externalId).toContain("job-123");});});
