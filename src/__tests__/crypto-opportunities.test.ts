import {describe,expect,it} from "vitest";import {assessCryptoOpportunity} from "../commerce/crypto-opportunities.js";
const base={source:"test",sourceId:"1",url:"https://example.com/job",title:"Fixed-scope data cleanup contract",body:"Contract project with defined acceptance criteria",company:"Acme"};
describe("crypto opportunity safety",()=>{it("accepts task work and excludes employment",()=>{expect(assessCryptoOpportunity(base).eligible).toBe(true);expect(assessCryptoOpportunity({...base,title:"Full-time engineer",body:"Permanent salary role"}).engagement).toBe("employment");});it.each([["Send your seed phrase","SEED_PHRASE_REQUEST"],["Connect your wallet to apply","WALLET_CONNECT_REQUEST"],["Pay an upfront deposit","UPFRONT_PAYMENT"],["Download setup.exe","EXECUTABLE_DOWNLOAD"],["Unlimited approval required","TOKEN_APPROVAL_REQUEST"]])("flags %s",(body,flag)=>expect(assessCryptoOpportunity({...base,body}).riskFlags).toContain(flag));it("deduplicates normalized equivalents",()=>{const a=assessCryptoOpportunity(base),b=assessCryptoOpportunity({...base,sourceId:"2",title:"  fixed-SCOPE   data cleanup CONTRACT "});expect(a.canonicalFingerprint).toBe(b.canonicalFingerprint);});it("handles 10k deterministic mixed cases",()=>{let eligible=0;for(let i=0;i<10000;i++){const x=assessCryptoOpportunity({...base,sourceId:String(i),body:i%7===0?"Connect your wallet to apply":"Freelance fixed-scope task"});if(x.eligible)eligible++;}expect(eligible).toBe(8571);});});
