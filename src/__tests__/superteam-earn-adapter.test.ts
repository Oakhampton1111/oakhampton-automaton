import {describe,expect,it,vi} from "vitest";import {PlatformRegistry} from "../commerce/platform-registry.js";import {SuperteamEarnAdapter} from "../commerce/superteam-earn-adapter.js";
describe("SuperteamEarnAdapter",()=>{const p=new PlatformRegistry().get("superteam-earn");it("is not ready without a key",async()=>{expect((await new SuperteamEarnAdapter(p,{}).health()).ready).toBe(false)});it("keeps only agent eligible listings and normalizes rewards",async()=>{const f=vi.fn(async()=>new Response(JSON.stringify({listings:[{id:"1",slug:"one",title:"AI bot",description:"Build an AI bot",agentAccess:"AGENT_ONLY",type:"BOUNTY",reward:1500,currency:"USDC"},{id:"2",title:"Human job",agentAccess:"HUMAN_ONLY"}]}),{status:200}));const a=new SuperteamEarnAdapter(p,{AUTOMATON_SUPERTEAM_EARN_API_KEY:"sk_test"},f as typeof fetch);const r=await a.discover();expect(r.opportunities).toHaveLength(1);expect(r.opportunities[0]).toMatchObject({sourceId:"1",budgetCents:150000,currency:"USDC"});expect(f).toHaveBeenCalledOnce()});it("blocks submissions unless external effects are enabled",async()=>{const a=new SuperteamEarnAdapter(p,{AUTOMATON_SUPERTEAM_EARN_API_KEY:"x"});await expect(a.submitApprovedAction({type:"submit",payload:{},idempotencyKey:"k"})).rejects.toThrow("EXTERNAL_EFFECTS_DISABLED")})});
