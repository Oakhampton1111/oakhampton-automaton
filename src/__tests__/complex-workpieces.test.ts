import {describe,expect,it} from "vitest";import {runComplexPortfolio} from "../backtest/complex-workpieces.js";
describe("four complex work-piece spreads",()=>{it("produces bounded and variable distributions",()=>{const reports=runComplexPortfolio(500,99);expect(reports).toHaveLength(4);for(const r of reports){expect(r.runs).toBe(500);expect(r.propose+r.review+r.reject).toBe(500);expect(r.expectedValue.minimum).toBeLessThan(r.expectedValue.maximum);expect(r.expectedValue.p10).toBeLessThanOrEqual(r.expectedValue.median);expect(r.expectedValue.median).toBeLessThanOrEqual(r.expectedValue.p90)}});it("contains no unconditional all-accept work piece",()=>{for(const r of runComplexPortfolio(500,100))expect(r.reject+r.review).toBeGreaterThan(0)});});
