import {describe,expect,it,vi} from "vitest";import {PlatformRegistry} from "../commerce/platform-registry.js";import {FreelancerApiAdapter} from "../commerce/freelancer-api-adapter.js";import {classifyProductionSkill} from "../quality/production.js";
describe("Freelancer API adapter",()=>{it("discovers multilingual public projects and adds classification hints",async()=>{const fetcher=vi.fn(async()=>new Response(JSON.stringify({status:"success",result:{projects:[{id:1,title:"Automatización de procesos con IA",status:"active",seo_url:"python/automatizacion",currency:{code:"EUR",is_escrowcom_supported:true},description:"Necesitamos integración de API y automatización del flujo de trabajo",type:"fixed",budget:{minimum:100,maximum:500},language:"es",nonpublic:false,local:false,hireme:false,bid_stats:{bid_count:2}}]}}),{status:200,headers:{"content-type":"application/json"}})) as any;const a=new FreelancerApiAdapter(new PlatformRegistry().get("freelancer"),{},fetcher);const r=await a.discover();expect(r.opportunities[0]).toMatchObject({sourceId:"1",language:"es",currency:"EUR",budgetCents:50000});expect(classifyProductionSkill(r.opportunities[0])).toBe("software");});it("requires OAuth before bidding",async()=>{const a=new FreelancerApiAdapter(new PlatformRegistry().get("freelancer"),{});await expect(a.submitApprovedAction({type:"submit_proposal",payload:{proposal:{sourceId:"1"}},idempotencyKey:"x"})).rejects.toThrow("FREELANCER_OAUTH_TOKEN_MISSING");});});
